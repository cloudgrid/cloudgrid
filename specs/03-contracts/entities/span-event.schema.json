{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://spec/cloudgrid/ent/ENT-004",
  "title": "SpanEvent",
  "type": "object",
  "additionalProperties": false,
  "required": ["name", "timestamp", "timestampUnixNano", "attributes"],
  "properties": {
    "name": { "type": "string", "description": "OpenTelemetry span event name." },
    "timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "Event timestamp in UTC."
    },
    "timestampUnixNano": {
      "type": "string",
      "pattern": "^[0-9]+$",
      "description": "Event timestamp as OpenTelemetry Unix nanoseconds."
    },
    "attributes": {
      "type": "object",
      "description": "Event attributes.",
      "additionalProperties": true
    }
  }
}
