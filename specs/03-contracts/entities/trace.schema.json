{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://spec/cloudgrid/ent/ENT-001",
  "title": "Trace",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "startedAt", "startedAtUnixNano", "attributes"],
  "properties": {
    "id": { "type": "string", "description": "OpenTelemetry trace ID as lowercase hex." },
    "serviceName": {
      "type": "string",
      "description": "Primary service.name resource attribute when known."
    },
    "startedAt": {
      "type": "string",
      "format": "date-time",
      "description": "Trace start time in UTC."
    },
    "startedAtUnixNano": {
      "type": "string",
      "pattern": "^[0-9]+$",
      "description": "Trace start time as OpenTelemetry Unix nanoseconds. Read models must preserve this value for waterfall precision."
    },
    "endedAt": {
      "type": "string",
      "format": "date-time",
      "description": "Trace end time in UTC when known."
    },
    "endedAtUnixNano": {
      "type": "string",
      "pattern": "^[0-9]+$",
      "description": "Trace end time as OpenTelemetry Unix nanoseconds when known."
    },
    "durationNano": {
      "type": "string",
      "pattern": "^[0-9]+$",
      "description": "Trace duration in nanoseconds when known."
    },
    "durationMs": {
      "type": "number",
      "minimum": 0,
      "description": "Trace duration in milliseconds."
    },
    "rootSpanId": { "type": "string", "description": "Root span ID when known." },
    "status": {
      "type": "string",
      "enum": ["ok", "error", "unset"],
      "description": "Aggregated trace status."
    },
    "attributes": {
      "type": "object",
      "description": "Merged selected resource and trace-level attributes.",
      "additionalProperties": true
    }
  }
}
