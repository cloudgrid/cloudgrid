---
import { render } from "astro:content";
import HandbookLayout from "../../components/handbook/HandbookLayout.astro";
import { findHandbookEntry } from "../../lib/handbook";

const entry = await findHandbookEntry("adapters");
if (!entry) throw new Error("handbook/adapters overview is missing");
const { Content, headings } = await render(entry);
---

<HandbookLayout
  title={entry.data.title}
  description={entry.data.description}
  eyebrow={entry.data.eyebrow}
  accent={entry.data.accent}
  updated={entry.data.updated}
  headings={headings}
  currentSlug="/handbook/adapters"
>
  <Content />
</HandbookLayout>
