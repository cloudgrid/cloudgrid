# Wave 02 Parallel Services

Implement storage-write persistence and storage-read query semantics for v2.
The two tickets have disjoint service ownership and can run in parallel after
the BFF contract layer is aligned.

Tickets:

- `AIEV2-202`
- `AIEV2-203`
