# Wave 03 Runner And Frontend

Implement runner orchestration and frontend workspace migration. Runner depends
on storage persistence/query behavior. Frontend depends on the BFF contract
surface and can use mocked GraphQL responses for focused tests.

Tickets:

- `AIEV2-204`
- `AIEV2-205`
