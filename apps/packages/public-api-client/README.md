# @cloudgrid/public-api-client

Shared public API client for CloudGrid browser code, tooling, and integration scenarios.

This package owns executable GraphQL operation documents, operation names, typed request helpers, and endpoint metadata. Frontend routes and end-to-end scenarios import this package instead of defining their own GraphQL documents.
