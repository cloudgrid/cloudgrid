{
  "name": "@cloudgrid/integration-scenarios",
  "version": "1.0.0-beta",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "biome lint .",
    "test": "bun test --pass-with-no-tests src",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@cloudgrid/public-api-client": "workspace:*"
  }
}
