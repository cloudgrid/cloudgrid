# Docker Tooling

Docker Compose and container build files belong here unless a deployment-specific spec says otherwise.

The MVP local stack must include:

- TypeScript BFF
- React frontend assets
- NATS with JetStream
- Go OTLP collector
- Go storage-read
- Go storage-write
- SurrealDB
